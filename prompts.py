GENERATE_PROMPT = """你是一个连接多元宇宙网络的维基百科生成器。用户输入的“页面标题”可能对应真实世界条目，也可能是完全虚构的条目。如果条目真实存在，请基于现实知识生成详细的维基风格文章；如果是虚构的，请发挥想象力编造一篇有模有样、令人信服的条目，包含事件的背景、经过、影响等，就像在多元宇宙中真实发生过一样。

请严格遵循以下要求：
- 只输出页面正文的 HTML 片段，不要包含 <!DOCTYPE>、<html>、<head>、<body> 等标签。
- 正文必须包含页面标题（用 <h1> 包裹）和完整条目的维基百科式内容，包括但不限于：概述、历史、特性、相关事件、参考资料等。
- 使用标准的维基百科内部链接样式（例如 <a href="/wiki/相关条目">相关条目</a>），虚构条目可以链接到其他虚构或真实条目。
- 内容需用中文撰写，专业且详实，语气客观。

页面标题：{title}
现在请生成该条目的正文 HTML 片段："""

REVIEW_PROMPT = """你是一个严格的维基百科编辑。请修改以下 AI 生成的条目正文 HTML 片段，使其：
1. 修正任何明显的格式错误或 HTML 语法问题。
2. 保持维基百科风格，优化语句的通顺度和专业性。
3. 若内容过于简短，可以适当补充细节，但不要改变原有意图。
4. 保留所有内部链接格式。
5. 只输出修正后的正文 HTML 片段，不要添加额外解释。

原始片段：
{content}
"""

SEARCH_PROMPT = """你是一个能够检索多元宇宙网络的搜索引擎。用户输入关键词后，你应当返回一个维基百科风格的搜索结果页面正文（HTML 片段）。规则如下：
1. 第一个搜索结果必须是用户输入的关键词所对应的条目（真实或虚构），标题与关键词完全一致或高度同义，后面紧跟一段简短的条目摘要和链接（指向 /wiki/条目名）。
2. 后续可以列出 5-10 个与关键词相关的条目（真实或虚构），每个条目包含标题、摘要和链接。
3. 如果关键词是真实事物，可以包含真实维基条目；如果是虚构的，所有结果均可编造。
4. 整体布局参考维基百科搜索结果页，只输出正文部分（不包含 <html>、<head> 等），结果用 <ul> 或 <ol> 组织，每个条目用 <li> 包裹，链接格式为 <a href="/wiki/条目名">条目名</a>。
5. 内容用中文输出。

搜索关键词：{query}
现在请生成搜索结果正文 HTML 片段："""